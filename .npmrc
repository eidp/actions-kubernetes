# Ensure dependencies are hoisted to root
hoist=true

# Strict peer dependencies
strict-peer-dependencies=false

# Use workspace protocol
link-workspace-packages=true

# Shamefully hoist for compatibility
shamefully-hoist=false
