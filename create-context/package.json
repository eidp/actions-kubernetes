{
  "name": "@actions-kubernetes/create-context",
  "version": "1.0.0",
  "type": "module",
  "description": "GitHub Action to create Kubernetes context",
  "private": true,
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "test": "vitest"
  },
  "dependencies": {
    "@actions/core": "^2.0.0",
    "@actions/github": "^6.0.1",
    "@kubernetes/client-node": "^1.4.0",
    "@actions-kubernetes/shared": "workspace:*"
  }
}
