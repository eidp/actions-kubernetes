{
  "name": "@actions-kubernetes/verify-up",
  "version": "1.0.0",
  "type": "module",
  "description": "GitHub Action to verify Kubernetes resources are ready",
  "private": true,
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "test": "vitest"
  },
  "dependencies": {
    "@actions/core": "^2.0.0",
    "@actions/github": "^6.0.1",
    "@kubernetes/client-node": "^1.4.0",
    "parse-duration": "^2.1.4",
    "@actions-kubernetes/shared": "workspace:*",
    "@actions-kubernetes/k8s-client": "workspace:*"
  }
}
