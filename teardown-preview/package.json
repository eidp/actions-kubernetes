{
  "name": "@actions-kubernetes/teardown-preview",
  "version": "1.0.0",
  "description": "GitHub Action to teardown preview environments",
  "private": true,
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "@kubernetes/client-node": "^1.4.0",
    "parse-duration": "^2.1.4",
    "@actions-kubernetes/shared": "workspace:*"
  }
}
